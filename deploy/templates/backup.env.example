# Choose a repository on a different machine (SFTP, S3, etc.).
RESTIC_REPOSITORY=
RESTIC_PASSWORD_FILE=@config_dir@/restic-password
# Add provider credentials here if needed. Keep this file root-owned, mode 0600.
